# DavPosFront Public Bundle

Feed publico de actualizacion para el frontend empaquetado de Dav Punto de Venta.

Electron lee `latest.json`, descarga los archivos de `browser/`, valida SHA-256 y sirve el bundle actualizado desde disco local.
